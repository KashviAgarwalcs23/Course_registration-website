
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SkillGateway - Course Registration</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@700&family=Open+Sans:wght@300&display=swap" rel="stylesheet"> <!-- Google Fonts -->
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial', sans-serif;
      background-color: #ecf0f1; /* Soft light gray */
      scroll-behavior: smooth; /* Enable smooth scrolling globally */
    }

    /* Top Navigation Bar */
    header {
      background-color: #2c3e50; /* Dark gray for header */
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
    }

    .website-name {
      font-family: 'Roboto', sans-serif;
      color: white;
      font-size: 36px;
      font-weight: 700;
    }

    .navbar {
      display: flex;
    }

    .navbar ul {
      list-style: none;
      display: flex;
    }

    .navbar li {
      margin-left: 20px;
    }

    .navbar a {
      color: white;
      text-decoration: none;
      font-size: 16px;
      font-weight: bold;
      padding: 8px 12px;
      transition: background-color 0.3s ease;
    }

    .navbar a:hover {
      background-color: #27ae60; /* Soft green for hover */
      border-radius: 4px;
    }

 .notification-bar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background-color: #16a085;  /* Soft teal */
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 18px;
      font-weight: bold;
      display: none;  /* Initially hidden */
      opacity: 0;  /* Make it invisible initially */
      transition: opacity 1s ease, top 1s ease;  /* Smooth fade-in and slide-down */
      z-index: 1000;  /* Ensure it's on top of other elements */
    }

    /* Content Section with Background Image */
    .content-section {
      width: 100%;
      min-height: 500px; /* Adjust height based on your content */
      background-image: url('https://cdn.prod.website-files.com/64760069e93084646c9ee428/649ef6799df88b07dbece820_code-html-vs-css%20(1)-p-1600.jpg');
      background-size: cover; /* Make the image cover the section */
      background-position: center center; /* Center the image */
      background-repeat: no-repeat; /* Prevent the image from repeating */
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 50px 0;
      color: white;
    }

    .content-section p {
      font-size: 24px;
      font-weight: 600;
    }

    .content-section button {
      background-color: #f39c12; /* Bright orange for buttons */
      color: white;
      font-size: 16px;
      padding: 12px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .content-section button:hover {
      background-color: #e67e22; /* Slightly darker orange for hover */
    }

    /* Quiz Section */
    .quiz-section {
      width: 100%;
      padding: 50px 20px;
      background-color: #ffffff; /* White background */
      text-align: center;
      opacity: 0;
      transform: translateY(20px); /* Slightly below its default position */
      transition: opacity 1s ease, transform 1s ease;
      display: none; /* Hidden initially */
    }

    .quiz-section.visible {
      display: block; /* Make it visible */
      opacity: 1; /* Fade-in effect */
      transform: translateY(0); /* Move to its original position */
    }

    .quiz-section h2 {
      font-family: 'Roboto', sans-serif;
      color: #16a085; /* Teal color */
      font-size: 32px;
      font-weight: 700;
      margin-bottom: 20px;
    }

    .quiz-section p {
      font-family: 'Open Sans', sans-serif;
      color: #7f8c8d; /* Neutral gray */
      font-size: 18px;
      margin-bottom: 30px;
    }

    .quiz-section .quiz-options {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
    }

    .quiz-option {
      width: 200px;
      padding: 20px;
      background-color: #ecf0f1; /* Light gray */
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      cursor: pointer;
      transition: transform 0.3s ease, background-color 0.3s ease;
    }

    .quiz-option:hover {
      transform: translateY(-5px);
      background-color: #bdc3c7; /* Slightly darker gray */
    }

    .quiz-option img {
      width: 100%;  
      height: 200px;
      object-fit : cover;
      transition : transform 0.3s ease;
      border-radius: 8px;
    }

    .quiz-option span {
      display: block;
      margin-top: 10px;
      font-family: 'Roboto', sans-serif;
      font-size: 16px;
      font-weight: 700;
      color: #2c3e50; /* Dark gray */
    }
    
  </style>
</head>
<body>

<!-- Notification Bar (Welcome Message) -->
  <div class="notification-bar">
    <!--<span class="message">Welcome to SkillGateway! Start your learning journey today!</span>-->
    <div class="website-name">Welcome to SkillGateway! Start your learning journey today! </div>
  </div>

  <!-- Top Navigation Bar -->
  <header>
  <div class="website-name">SkillGateway</div>
  <nav class="navbar">
      <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/AboutUs">AboutUs</a></li>
          {{#if user}}
              <li><a href="/dashboard">Dashboard</a></li>
              <li><a href="/logout">Logout</a></li>
          {{else}}
              <li><a href="/login">Login</a></li>
              <li><a href="/register">Register</a></li>
          {{/if}}
      </ul>
  </nav>
</header>


  <!-- Content Section with Background Image -->
  <div class="content-section">
    <p>The best efficient free platform to start your learning journey</p>
    <button onclick="showQuiz()">Explore Now</button>
  </div>

  <!-- Quiz Section -->
  <div id="quiz-section" class="quiz-section">
    <h2>What's Your Learning Style?</h2>
    <div class="quiz-options">
      <div class="quiz-option">
        <img src="https://img.freepik.com/free-photo/boy-fantasy-world_23-2151732317.jpg" alt="Visual Learner">
        <span>Visual Learner</span>
      </div>
      <div class="quiz-option">
        <img src="https://img.freepik.com/premium-photo/girl-reading-book-room-with-headphones_662214-38702.jpg?w=740" alt="Auditory Learner">
        <span>Auditory Learner</span>
      </div>
      <div class="quiz-option">
        <img src="https://cdn.prod.website-files.com/5b69a01ba2e409501de055d1/66e1535077e6769e1ed14185_664f597075b2bd6c22d5608b_Hands-on%2520learning%2520experiences%2520for%2520promoting%2520critical%2520thinking%2520skills.webp" alt="Hands-On Learner">
        <span>Hands-On Learner</span>
      </div>
    </div>
  </div>

  <script>
    function showQuiz() {
      const quizSection = document.getElementById('quiz-section');
      quizSection.style.display = 'block'; // Make the section block-level
      setTimeout(() => {
        quizSection.classList.add('visible'); // Trigger fade-in effect
      }, 100);
      quizSection.scrollIntoView({ behavior: 'smooth' }); // Smooth scroll to the quiz section
    }
  </script>

</body>